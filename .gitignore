# Generated learning outputs (personal data — not committed)
*.env
*.env.*
.env.local

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor files
.cursor/
.vscode/settings.json
*.swp
*.swo

# When this repo is cloned to ~/.agents/skills/ai-learning, generated files sit next to SKILL.md:
*-day-*.html
*-manifest.json
deliver.py
delivery.log
delivery.stdout.log
delivery.stderr.log
quiz-*.md
*-curriculum.md
*-curriculum.html

# If you ever copy learning files into this repo by mistake — never commit credentials
smtp-config.json
.smtp-config.json
.user-profile.json

# Internal backlog — keep local only (not published with the repo)
TODOS.md
